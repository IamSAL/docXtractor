# DocXtractor TODOs

Generated by `/plan-ceo-review` — 2026-04-18. Last updated: 2026-04-18.

---

## P3 — Hygiene / future-proofing (open)

### Demo page: show actual extraction
**File**: `client/src/routes/demo.tsx`
**Status**: In progress — DemoModule implemented, frontend wizard in progress.

Still shows only a JSON Schema Builder. The "View Demo" CTA on the landing page leads here. Replace with an end-to-end demo: upload a sample PDF → run extraction → show structured JSON output. Complex UI work — deferred.

---

### Run re-fetching in extraction path
**File**: `server/src/runs/runs.service.ts:484-491`

The `handleDocumentParsed` handler re-fetches the `Run` entity after an atomic `jsonb_set` update to get true state. Necessary to avoid stale data, but adds a DB round-trip per parsed document. Future optimisation: use a `RETURNING` clause on the update query to eliminate the separate `findOne`.

---

## Done (fixed in review sessions 2026-04-18)

### Security / P1 fixes
- [x] Webhook endpoint — removed `@Public()`, added JWT auth + ownership check via `findById`
- [x] ValidationPipe — enabled `whitelist: true`, `forbidNonWhitelisted: true`, `transform: true`
- [x] Extractor ownership — added `userId` (nullable UUID FK) + `isPublic` (default `false`) to entity; all CRUD enforces ownership; seeded extractors have `isPublic: true, userId: null`
- [x] LLM refusal detection — regex patterns checked before `jsonrepair`; throws instead of storing garbage JSON
- [x] Backup service shell injection — `execSync` shell string → `execFileSync` + `gzipSync` (no shell interpolation)
- [x] TypeORM `synchronize: true` hardcoded — now `configService.get('NODE_ENV') !== 'production'`
- [x] SSRF at DTO level — `@IsUrl({ protocols: ['http','https'], require_protocol: true })` on `RunSourceDto.url`
- [x] SSRF IP blocklist in parser — `_check_ssrf()` resolves hostname and blocks RFC1918/loopback/link-local before fetching; applied to both `process_job` and `process_example_source_job`

### Quality / P2 fixes
- [x] Password reset email — was a `void resetToken` TODO; now sends real reset link via `MailService`
- [x] `@ArrayMinSize(1)` added to `sources` in `CreateRunDto` — prevents empty-source runs
- [x] `logger.log(fullPrompt)` → `logger.debug(fullPrompt)` — stops document PII appearing in info logs
- [x] Health check endpoint — new `GET /health` (public) returning `{ status, db }`
- [x] Deploy script — replaced `sleep 15` + single check with retry loops (5s/90s) + API smoke test on `/health`
- [x] Dev tools in production — `TanStackDevtools` wrapped in `import.meta.env.DEV` guard
- [x] Extractor `isPublic` toggle in UI — `Switch` added to Basic Information section; field added to `ExtractorFormData` type and defaults
- [x] Dead code in `auth.service.ts` — removed `const hasProfile = false` and `const fullName = ''` variable declarations; values inlined directly
- [x] Request correlation IDs — `run_id` already threaded through all 4 BullMQ payloads and logged in both Python workers at every stage

### Frontend / UX fixes
- [x] Landing page fake social proof removed — "Join 5,000+ companies" → "Open source and self-hosted"
- [x] Dead "Get Access" email form removed — replaced with `<Link to="/login">Get Started</Link>` + GitHub link
- [x] "Review Flagged Item" and "Explore Documentation" dead buttons removed
- [x] 9 dead `href="#"` footer links replaced — real GitHub/Swagger destinations; Legal column removed
- [x] Copyright fixed — `© 2024 DocXTractor Inc.` → `© {new Date().getFullYear()} DocXTractor`
- [x] CSS 404 (QA ISSUE-001) — confirmed stale deployment artifact; resolved by next CI/CD rebuild; no source fix needed
- [x] Demo page made public — no auth required (QA ISSUE-004)
- [x] Invalid login shows real error — not 'No refresh token available' (QA ISSUE-002)

### Hygiene
- [x] `server/src/chat/` deleted — dead module, never imported in app.module.ts
- [x] `client/src/routes/demo/` boilerplate deleted — TanStack Start starter kit routes removed (19 files)
- [x] Orval API client regenerated — all backend endpoint/DTO changes reflected in `src/api/`
- [x] Stale `client/src/api/endpoints/chat/` deleted — chat module removed from server
- [x] Hand-written client source lint-clean (Biome)
